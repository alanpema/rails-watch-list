<h1 class="text-white">list god</h1>
<%= simple_form_for [@lists] do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.submit "Create new list", class: "btn btn-warning rounded-pill text-white" %>
<% end %>
